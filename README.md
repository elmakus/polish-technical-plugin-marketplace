# Elmakus Plugins — single-plugin marketplace

This repository is a single-plugin marketplace containing `polish-technical-assistant`.

The marketplace manifest is at `.agents/plugins/marketplace.json` and points to the local plugin folder `./plugins/polish-technical-assistant`.

The plugin uses the portable Agent Plugins root-level `plugin.json` format and contains only a skill.
